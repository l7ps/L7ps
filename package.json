{
  "name": "vida-cotidiana-partners-dashboard",
  "version": "1.0.0",
  "description": "Dashboard para análise de parceiros da API Vida Cotidiana",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["api", "dashboard", "partners", "vida-cotidiana"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}